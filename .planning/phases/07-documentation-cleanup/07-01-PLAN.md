---
phase: 07-documentation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/core/Camera.ts, src/core/DataProvider.ts]
autonomous: true

must_haves:
  truths:
    - "Camera class has comprehensive JSDoc describing coordinate system"
    - "All public Camera methods have @param and @returns tags with explicit descriptions"
    - "DataProvider TODO is resolved or updated with issue reference"
  artifacts:
    - path: "src/core/Camera.ts"
      provides: "Camera documentation with coordinate system details"
      contains: "class JSDoc with coordinate system documentation"
    - path: "src/core/DataProvider.ts"
      provides: "Resolved TODO comment"
      contains: "no TODO comment or TODO with issue reference"
  key_links:
    - from: "src/core/Camera.ts"
      to: "Phase 1/1.1 coordinate system"
      via: "JSDoc class-level documentation"
      pattern: "@see.*Phase.*coordinate|Y-up.*convention|Right-handed"
    - from: "src/core/Camera.ts"
      to: "gl-matrix quaternion methods"
      via: "JSDoc references"
      pattern: "@see.*quat|vec3.*transformQuat|gl-matrix"
---

<objective>
Add comprehensive documentation to Camera class and resolve technical debt in DataProvider.

Purpose: Complete documentation requirements from milestone audit, improving code maintainability and developer onboarding.
Output: Well-documented Camera class with coordinate system explanation, resolved TODO in DataProvider
</objective>

<execution_context>
@/home/frater260/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/frater260/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-documentation-cleanup/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add class-level JSDoc to Camera.ts with coordinate system documentation</name>
  <files>src/core/Camera.ts</files>
  <action>
    Add comprehensive class-level JSDoc comment to Camera class (before export class Camera):

    ```
    /**
     * Quaternion-based camera for 3D WebGL rendering.
     *
     * Uses gl-matrix quaternions for orientation to eliminate gimbal lock at extreme angles.
     * Movement is in local camera space (forward, right, up vectors derived from orientation),
     * not fixed world up vector.
     *
     * Coordinate system: Y-up, right-handed WebGL conventions.
     * - Y-axis points up
     * - Positive Z points out of screen (toward viewer)
     * - Positive X points right
     *
     * Implementation references:
     * - Phase 1: Euler rotation fix (CAM-01, CAM-02)
     * - Phase 1.1: Quaternion migration (CAM-03)
     *
     * @see https://glmatrix.net/docs/ for gl-matrix library
     */
    ```

    Include:
    - Quaternion-based orientation explanation (no gimbal lock)
    - Local coordinate space movement (no fixed up vector)
    - Y-up convention and right-handed WebGL system
    - Forward/right/up axes derived from quaternion transformations
    - Reference to Phase 1 and 1.1 implementation decisions

    Place JSDoc immediately before `export class Camera` (line 13).
    Use plain text in JSDoc (no markdown formatting).
  </action>
  <verify>grep -A 20 "export class Camera" src/core/Camera.ts | grep -q "quaternion\|Y-up\|coordinate system"</verify>
  <done>Camera class has JSDoc describing quaternion-based orientation and Y-up coordinate system</done>
</task>

<task type="auto">
  <name>Task 2: Add @param and @returns JSDoc to key Camera methods</name>
  <files>src/core/Camera.ts</files>
  <action>
    For each public method, add or enhance JSDoc with @param and @returns tags:

    **Public properties to document (already have comments, convert to JSDoc):**
    - position: vec3 - Camera position in world space
    - distance: number - Distance from look target
    - moveSpeed: number - Base movement speed
    - fastMoveMultiplier: number - Multiplier when shift is held
    - mouseSensitivity: number - Mouse rotation sensitivity
    - zoomSpeed: number - Mouse wheel zoom speed
    - fov: number - Vertical field of view in degrees
    - near: number - Near clipping plane distance
    - far: number - Far clipping plane distance

    **Public methods to add @param/@returns:**

    1. handleKeyEvent(key: string, pressed: boolean): void
       - @param key - Keyboard key code
       - @param pressed - Whether key is pressed (true) or released (false)

    2. handleMouseMove(deltaX: number, deltaY: number): void
       - @param deltaX - Horizontal mouse movement in pixels
       - @param deltaY - Vertical mouse movement in pixels

    3. handleMouseWheel(delta: number): void
       - @param delta - Mouse wheel scroll delta (positive = zoom in, negative = zoom out)

    4. update(deltaTime: number): void
       - @param deltaTime - Time since last frame in seconds (default: 1/60)

    5. reset(): void
       - @returns void (explicitly stated for consistency)

    6. getShaderUniforms(aspect: number): object
       - @param aspect - Canvas aspect ratio (width / height)
       - @returns Object with shader uniforms: { u_cameraPosition, u_viewMatrix, u_mvpMatrix, u_fov, u_near, u_far, u_aspect }

    7. getForward(): vec3
       - @returns Forward direction vector in world space

    8. getRight(): vec3
       - @returns Right direction vector in world space

    9. getUp(): vec3
       - @returns Up direction vector in world space

    10. toDebugInfo(): object
       - @returns Debug info object with position (x, y, z), rotation (x, y), distance

    Pattern to follow (from DataProvider.ts):
    ```
    /**
     * [Brief description of what method does]
     *
     * @param paramName - [Explicit description, not just type]
     * @returns [Explicit description of what's returned]
     */
    ```

    Keep descriptions explicit (not just rely on TypeScript types).
    Do NOT add usage examples in JSDoc (per CONTEXT.md decision).
  </action>
  <verify>grep -c "@param" src/core/Camera.ts && grep -c "@returns" src/core/Camera.ts</verify>
  <done>Every public Camera method has @param and @returns tags with explicit descriptions</done>
</task>

<task type="auto">
  <name>Task 3: Resolve TODO comment in DataProvider.ts</name>
  <files>src/core/DataProvider.ts</files>
  <action>
    Locate TODO comment in DataProvider.ts (line 56):
    `TODO: Implement your own data generation/loading logic here`

    This TODO is inside JSDoc for `getPointData()` static method.
    The current codebase already has:
    - Generated point data (via `generatePoints()` in CameraPlayground.vue)
    - JSON file loading (via `loadFromFile()`)
    - SQLite database loading (via `loadSqliteFile()`)

    The TODO was a placeholder for implementing data generation/loading.
    Since all data loading methods are already implemented, this TODO is no longer applicable.

    Action: Remove the entire TODO comment line from the JSDoc.

    Remove line:
    `* TODO: Implement your own data generation/loading logic here`

    Preserve all other JSDoc content (Expected format, @returns tag).
  </action>
  <verify>! grep -q "TODO" src/core/DataProvider.ts</verify>
  <done>DataProvider.ts has no TODO comments</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. Camera class JSDoc mentions coordinate system and references Phase 1/1.1
2. All public methods in Camera.ts have @param and @returns tags
3. DataProvider.ts has no TODO comments remaining
4. JSDoc style matches DataProvider.ts patterns (@param with explicit descriptions, @returns)
</verification>

<success_criteria>
- Camera.ts has class-level JSDoc with coordinate system documentation
- All public Camera methods have @param and @returns JSDoc tags with explicit descriptions
- DataProvider.ts has no remaining TODO comments
- Documentation follows established JSDoc patterns from DataProvider.ts
- No new TODO comments introduced
</success_criteria>

<output>
After completion, create `.planning/phases/07-documentation-cleanup/07-01-SUMMARY.md`
</output>
