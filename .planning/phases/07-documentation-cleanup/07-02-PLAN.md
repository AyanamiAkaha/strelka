---
phase: 07-documentation-cleanup
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [src/components/WebGLPlayground.vue, src/components/DebugInfo.vue, src/core/ShaderManager.ts]
autonomous: true

must_haves:
  truths:
    - "Camera consumers have @see cross-references to Camera.ts JSDoc"
    - "Developers can navigate from camera usage to camera documentation"
  artifacts:
    - path: "src/components/WebGLPlayground.vue"
      provides: "@see reference to Camera.ts"
      contains: "@see Camera"
    - path: "src/components/DebugInfo.vue"
      provides: "@see reference to Camera.ts"
      contains: "@see Camera"
    - path: "src/core/ShaderManager.ts"
      provides: "@see reference to Camera.ts"
      contains: "@see Camera"
  key_links:
    - from: "src/components/WebGLPlayground.vue"
      to: "src/core/Camera.ts"
      via: "@see JSDoc tag in camera initialization"
      pattern: "@see.*Camera|//.*see.*Camera"
    - from: "src/components/DebugInfo.vue"
      to: "src/core/Camera.ts"
      via: "@see JSDoc tag in debug info component"
      pattern: "@see.*Camera"
    - from: "src/core/ShaderManager.ts"
      to: "src/core/Camera.ts"
      via: "@see JSDoc tag in shader uniform usage"
      pattern: "@see.*Camera"
---

<objective>
Add cross-references from camera consumers to Camera.ts documentation.

Purpose: Enable developers to navigate from camera usage code to the authoritative coordinate system documentation in Camera.ts.
Output: @see references in camera-consuming files pointing back to Camera.ts as source of truth
</objective>

<execution_context>
@/home/frater260/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/frater260/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-documentation-cleanup/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @see Camera references in WebGLPlayground.vue</name>
  <files>src/components/WebGLPlayground.vue</files>
  <action>
    Locate camera initialization and usage in WebGLPlayground.vue.
    Add @see reference in JSDoc comments where Camera is used.

    Search for:
    - Camera import statement
    - Camera instantiation (e.g., `const camera = new Camera()`)
    - Camera method calls (e.g., `camera.update()`, `camera.handleMouseMove()`, etc.)
    - Camera property access (e.g., `camera.position`, `camera.getShaderUniforms()`)

    Add JSDoc comment above camera initialization:
    ```
    /**
     * Camera instance for 3D viewport navigation.
     * @see Camera - Quaternion-based camera with Y-up coordinate system
     */
    const camera = new Camera()
    ```

    For existing JSDoc comments on camera-related functions/methods:
    Add @see Camera at end of comment block:
    ```
    /**
     * Handle camera movement and rotation
     * @see Camera
     */
    ```

    Place @see reference at the component level (near camera instantiation) or in method JSDoc.
    Use format: `@see Camera` (not file path, class name is sufficient).
  </action>
  <verify>grep -q "@see Camera" src/components/WebGLPlayground.vue</verify>
  <done>WebGLPlayground.vue has @see Camera reference in JSDoc</done>
</task>

<task type="auto">
  <name>Task 2: Add @see Camera references in DebugInfo.vue</name>
  <files>src/components/DebugInfo.vue</files>
  <action>
    Locate camera usage in DebugInfo.vue (likely reading camera state via props).
    DebugInfo.vue receives camera debug info (position, rotation, distance).

    Find the props or computed properties related to camera:
    - Look for props like `position`, `rotation`, `distance`
    - Look for computed properties that access camera state

    Add JSDoc comment with @see reference:
    ```
    /**
     * Debug display for camera state
     * @see Camera.toDebugInfo() - Method providing debug data
     * @see Camera - Camera class with coordinate system documentation
     */
    ```

    Place JSDoc at component level (before `<script setup>` or in component definition).

    Use format: `@see Camera` and/or `@see Camera.toDebugInfo()` for method-specific references.
  </action>
  <verify>grep -q "@see Camera" src/components/DebugInfo.vue</verify>
  <done>DebugInfo.vue has @see Camera reference in JSDoc</done>
</task>

<task type="auto">
  <name>Task 3: Add @see Camera references in ShaderManager.ts</name>
  <files>src/core/ShaderManager.ts</files>
  <action>
    Locate camera-related code in ShaderManager.ts.
    ShaderManager likely receives camera uniforms (position, view matrix, MVP matrix) for rendering.

    Search for:
    - Camera imports or references
    - Shader uniform parameters related to camera (u_cameraPosition, u_viewMatrix, u_mvpMatrix, etc.)
    - Methods that accept camera uniforms (e.g., `setCameraUniforms()`, `render()` with camera parameter)

    Add JSDoc comment with @see reference:
    ```
    /**
     * Set camera-related shader uniforms
     * @see Camera.getShaderUniforms() - Method providing uniforms
     * @see Camera - Camera class with coordinate system documentation
     */
    ```

    If method signature includes camera parameter:
    ```
    /**
     * Render scene with camera transformation
     * @param uniforms - Shader uniforms including camera position and matrices
     * @see Camera.getShaderUniforms()
     * @see Camera
     */
    ```

    Place JSDoc at method level (before method definition).
    Use format: `@see Camera` and method-specific references like `@see Camera.getShaderUniforms()`.
  </action>
  <verify>grep -q "@see Camera" src/core/ShaderManager.ts</verify>
  <done>ShaderManager.ts has @see Camera reference in JSDoc</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. WebGLPlayground.vue has @see Camera reference
2. DebugInfo.vue has @see Camera reference
3. ShaderManager.ts has @see Camera reference
4. All @see references are in JSDoc comments (not inline code comments)
5. No broken @see references (Camera class exists)
</verification>

<success_criteria>
- Camera consumers (WebGLPlayground, DebugInfo, ShaderManager) have @see Camera references
- @see references point to Camera.ts as coordinate system documentation source
- Developers can navigate from camera usage to Camera class JSDoc
- @see format follows standard JSDoc conventions
</success_criteria>

<output>
After completion, create `.planning/phases/07-documentation-cleanup/07-02-SUMMARY.md`
</output>
