---
phase: 01.1-implement-quaternion-based-camera
plan: 04
type: execute
wave: 3
depends_on: ["01.1-03"]
files_modified: ["src/core/Camera.ts"]
autonomous: true

must_haves:
  truths:
    - "getShaderUniforms() extracts forward/up vectors from quaternion"
    - "Shader receives same uniform format (no shader changes needed)"
    - "Movement in update() uses quaternion-based vectors"
  artifacts:
    - path: "src/core/Camera.ts"
      provides: "Shader uniform extraction from quaternion camera"
      contains: "getShaderUniforms"
  key_links:
    - from: "src/core/Camera.ts"
      to: "src/core/ShaderManager.ts"
      via: "getShaderUniforms() method"
      pattern: "getShaderUniforms\(aspect\)"
    - from: "src/core/ShaderManager.ts"
      to: "WebGL shaders"
      via: "u_cameraRotation uniform"
      pattern: "uniform vec2 u_cameraRotation"
---

<objective>
Update Camera.ts getShaderUniforms() to extract vectors from quaternion for shader compatibility. Preserve existing shader logic by providing same uniform format, extracting Euler angles or forward/up vectors from quaternion as needed.

Purpose: Maintain shader calculations (research recommendation) while using quaternion for rotation
Output: Shader receives camera parameters from quaternion-based camera without shader modifications
</objective>

<execution_context>
@/home/frater260/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/frater260/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-implement-quaternion-based-camera/01.1-RESEARCH.md
@.planning/phases/01.1-implement-quaternion-based-camera/01.1-CONTEXT.md

# Current shader expects Euler angles
@src/core/ShaderManager.ts
</context>

<tasks>

<task type="auto">
  <name>Extract Euler angles from quaternion for shader</name>
  <files>src/core/Camera.ts</files>
  <action>
    Update getShaderUniforms() to extract Euler angles from quaternion:

    Replace getShaderUniforms() method:

    getShaderUniforms(aspect: number) {
      // Extract Euler angles from quaternion for shader compatibility
      const pitch = Math.asin(2 * (this.orientation[3] * this.orientation[1] -
                                      this.orientation[0] * this.orientation[2]))
      const yaw = Math.atan2(2 * (this.orientation[3] * this.orientation[0] +
                                   this.orientation[1] * this.orientation[2]),
                            1 - 2 * (this.orientation[0] * this.orientation[0] +
                                         this.orientation[1] * this.orientation[1]))

      return {
        u_cameraPosition: [this.position[0], this.position[1], this.position[2]],
        u_cameraRotation: [pitch, yaw],  // Keep format for shader compatibility
        u_fov: this.fov * Math.PI / 180,
        u_near: this.near,
        u_far: this.far,
        u_aspect: aspect
      }
    }

    This extracts pitch and yaw from quaternion for shader compatibility.
    Shader's getForwardVector() expects [pitch, yaw] format.

    Alternative (if shader modification acceptable):
    Compute view matrix from quaternion using mat4.fromQuat() and pass to shader.
    For this phase, keep shader unchanged to minimize risk (research recommendation).
  </action>
  <verify>grep -E "getShaderUniforms|u_cameraRotation.*pitch.*yaw" src/core/Camera.ts shows Euler extraction</verify>
  <done>getShaderUniforms() extracts pitch/yaw from quaternion, preserves shader format</done>
</task>

<task type="auto">
  <name>Update movement to use quaternion vectors</name>
  <files>src/core/Camera.ts</files>
  <action>
    Update update() method to use quaternion-based vectors:

    In update() method, replace Euler-based vector calculations:

    OLD:
      const forward = new Vec3(
        Math.cos(this.rotation.x) * Math.sin(this.rotation.y),
        -Math.sin(this.rotation.x),
        -Math.cos(this.rotation.x) * Math.cos(this.rotation.y)
      )
      const right = new Vec3(
        Math.cos(this.rotation.y),
        0,
        -Math.sin(this.rotation.y)
      )
      const up = new Vec3(0, 1, 0)

    NEW:
      const forward = this.getForward()
      const right = this.getRight()
      const up = this.getUp()

    Movement application (WASD) remains the same - just use quaternion-based vectors.
    This fixes gimbal lock because movement vectors are camera's local axes, not world axes.

    Ensure up vector uses getUp() from quaternion, NOT fixed world up [0, 1, 0].
  </action>
  <verify>grep -E "update\(|getForward\(\)|getRight\(\)|getUp\(\)" src/core/Camera.ts shows update using getter methods</verify>
  <done>update() uses getForward/getRight/getUp() for quaternion-based movement vectors</done>
</task>

</tasks>

<verification>
- getShaderUniforms() extracts Euler angles from quaternion
- Shader uniform format unchanged (u_cameraRotation as [pitch, yaw])
- update() uses quaternion-based vector methods
</verification>

<success_criteria>
Camera integrates quaternion rotation with existing shader without shader modifications
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-implement-quaternion-based-camera/01.1-04-SUMMARY.md`
</output>
