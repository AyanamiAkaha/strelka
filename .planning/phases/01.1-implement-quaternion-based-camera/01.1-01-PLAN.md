---
phase: 01.1-implement-quaternion-based-camera
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["package.json", "src/core/Math.ts"]
autonomous: true

must_haves:
  truths:
    - "gl-matrix package is installed in dependencies"
    - "gl-matrix TypeScript types are available in the project"
    - "Math.ts exports gl-matrix vec3 for use in Camera.ts"
  artifacts:
    - path: "package.json"
      provides: "gl-matrix dependency declaration"
      contains: "gl-matrix"
    - path: "src/core/Math.ts"
      provides: "gl-matrix vec3 and quat exports"
      exports: ["vec3", "quat"]
  key_links: []
---

<objective>
Add gl-matrix dependency and re-export vector/quaternion types for Camera.ts use. This establishes the foundation for quaternion-based camera rotation by providing the required WebGL matrix math library with built-in TypeScript support.

Purpose: gl-matrix is the standard WebGL math library with optimized quaternion operations and TypeScript definitions
Output: gl-matrix installed, Math.ts exports vec3/quat types
</objective>

<execution_context>
@/home/frater260/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/frater260/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-implement-quaternion-based-camera/01.1-RESEARCH.md
@.planning/phases/01.1-implement-quaternion-based-camera/01.1-CONTEXT.md
@.planning/phases/01-camera-rotation-fix/01-camera-rotation-fix-03-SUMMARY.md

# Current codebase state
@src/core/Camera.ts
@src/core/Math.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Install gl-matrix dependency</name>
  <files>package.json</files>
  <action>
    Run: npm install gl-matrix@3.4.4

    This installs the WebGL-standard math library with:
    - quat module: quaternion operations (rotateX, rotateY, normalize, identity)
    - vec3 module: vector operations (transformQuat, add, subtract)
    - mat4 module: matrix operations (fromQuat, perspective, lookAt)
    - Built-in TypeScript definitions (no @types package needed)

    Do NOT install any @types package - gl-matrix includes TS definitions.
  </action>
  <verify>grep "gl-matrix" package.json shows version ^3.4.4 in dependencies</verify>
  <done>gl-matrix is listed in package.json dependencies with version ^3.4.4</done>
</task>

<task type="auto">
  <name>Export gl-matrix types from Math.ts</name>
  <files>src/core/Math.ts</files>
  <action>
    Replace entire Math.ts content with gl-matrix re-exports:

    import { vec3, quat } from 'gl-matrix'

    export { vec3, quat }

    This removes the custom Vec3 class and exports gl-matrix's vec3 and quat modules directly.
    Camera.ts will import vec3 and quat from Math.ts as before, but now they're gl-matrix types.

    Note: gl-matrix vec3/quat are modules (functions), not classes like the old Vec3.
    Usage changes from new Vec3(x,y,z) to vec3.fromValues(x,y,z) or vec3.create().
  </action>
  <verify>grep -E "(vec3|quat)" src/core/Math.ts shows only import and export statements</verify>
  <done>Math.ts exports vec3 and quat from gl-matrix (no custom Vec3 class remains)</done>
</task>

</tasks>

<verification>
- package.json includes gl-matrix dependency
- Math.ts exports vec3 and quat modules from gl-matrix
- TypeScript compilation succeeds (npm run type-check passes)
</verification>

<success_criteria>
gl-matrix dependency installed and Math.ts re-exports vec3/quat types, ready for Camera.ts to use quaternion operations
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-implement-quaternion-based-camera/01.1-01-SUMMARY.md`
</output>
