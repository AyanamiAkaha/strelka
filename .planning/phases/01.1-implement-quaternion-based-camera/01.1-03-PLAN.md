---
phase: 01.1-implement-quaternion-based-camera
plan: 03
type: execute
wave: 2
depends_on: ["01.1-01", "01.1-02"]
files_modified: ["src/core/Camera.ts"]
autonomous: true

must_haves:
  truths:
    - "Camera stores orientation as quaternion instead of Euler angles"
    - "Rotation accumulates using quat.rotateX() and quat.rotateY()"
    - "Forward/right/up vectors calculated using vec3.transformQuat()"
    - "Reset function uses quat.identity() to reset orientation"
  artifacts:
    - path: "src/core/Camera.ts"
      provides: "Quaternion-based camera rotation eliminating gimbal lock"
      contains: "private orientation: quat"
      contains: "quat.rotate"
  key_links:
    - from: "src/core/Camera.ts"
      to: "gl-matrix quat module"
      via: "import { quat } from './Math'"
      pattern: "quat\.(rotate|create|identity)"
---

<objective>
Implement quaternion-based camera rotation in Camera.ts to eliminate gimbal lock. Replace Euler angle rotation with quaternion orientation, using gl-matrix's quaternion operations for smooth accumulation and rotation vector calculations.

Purpose: Quaternions avoid gimbal lock at extreme angles, providing stable freeflight rotation
Output: Camera uses quaternion orientation, accumulates rotations without angle clamping
</objective>

<execution_context>
@/home/frater260/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/frater260/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-implement-quaternion-based-camera/01.1-RESEARCH.md
@.planning/phases/01.1-implement-quaternion-based-camera/01.1-CONTEXT.md

# Current Camera.ts uses Euler angles
@src/core/Camera.ts

# Phase 1 identified gimbal lock root cause
@.planning/phases/01-camera-rotation-fix/01-camera-rotation-fix-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Replace Euler angles with quaternion orientation</name>
  <files>src/core/Camera.ts</files>
  <action>
    Update Camera.ts to use quaternion instead of Euler angles:

    1. Add quaternion import:
       Update import to include quat:
       import { vec3, quat } from './Math'

    2. Replace rotation field with orientation:
       OLD: public rotation: { x: number, y: number }
       NEW: private orientation: quat

    3. Initialize quaternion in constructor:
       OLD: this.rotation = { x: 0, y: 0 }
       NEW: this.orientation = quat.create()  // Identity quaternion [0, 0, 0, 1]

    4. Update toDebugInfo():
       OLD: rotation: this.rotation
       NEW: rotation: this.quatToEuler(this.orientation)  // Helper for debug display

    5. Add quatToEuler() helper for debug:
       quatToEuler(q: quat): { x: number, y: number } {
         // Extract Euler angles from quaternion for display only
         const pitch = Math.asin(2 * (q[3] * q[1] - q[0] * q[2]))
         const yaw = Math.atan2(2 * (q[3] * q[0] + q[1] * q[2]),
                              1 - 2 * (q[0] * q[0] + q[1] * q[1]))
         return { x: pitch, y: yaw }
       }

    6. Update reset():
       OLD: this.rotation = { x: 0, y: 0 }
       NEW: quat.identity(this.orientation)  // Reset to identity quaternion

    Remove pitch clamping from handleMouseMove() - quaternions don't need it.
  </action>
  <verify>grep -E "private orientation.*quat|rotation.*{.*number.*number" src/core/Camera.ts shows orientation as quaternion</verify>
  <done>Camera stores orientation as quat, initialized with quat.create(), reset with quat.identity()</done>
</task>

<task type="auto">
  <name>Implement quaternion rotation accumulation</name>
  <files>src/core/Camera.ts</files>
  <action>
    Update handleMouseMove() to accumulate rotations using quaternion multiplication:

    Replace entire handleMouseMove() method:

    handleMouseMove(deltaX: number, deltaY: number): void {
      const pitchChange = deltaY * this.mouseSensitivity
      const yawChange = deltaX * this.mouseSensitivity

      // Apply pitch rotation
      const temp = quat.create()
      quat.rotateX(temp, this.orientation, pitchChange)

      // Apply yaw rotation to result (order matters: X then Y)
      quat.rotateY(this.orientation, temp, yawChange)

      // Normalize periodically to prevent quaternion drift
      quat.normalize(this.orientation, this.orientation)
    }

    Key points:
    - Use temporary quaternion for multi-step rotation (research Pitfall 3)
    - Apply pitch (mouse Y) then yaw (mouse X) - matches user expectations
    - Normalize after each rotation to prevent drift (research Pitfall 1)
    - Remove old angle clamping - quaternions handle all angles naturally
  </action>
  <verify>grep -E "(quat\.rotate|this\.rotation\.|clamp)" src/core/Camera.ts shows only quat.rotate, no Euler rotation</verify>
  <done>handleMouseMove() uses quat.rotateX/Y with normalization, no angle clamping</done>
</task>

<task type="auto">
  <name>Calculate vectors from quaternion orientation</name>
  <files>src/core/Camera.ts</files>
  <action>
    Update getForward(), getRight(), getUp() to derive vectors from quaternion:

    getForward(): vec3 {
      const forward = vec3.create()
      vec3.transformQuat(forward, [0, 0, -1], this.orientation)
      return forward
    }

    getRight(): vec3 {
      const right = vec3.create()
      vec3.transformQuat(right, [1, 0, 0], this.orientation)
      return right
    }

    getUp(): vec3 {
      const up = vec3.create()
      vec3.transformQuat(up, [0, 1, 0], this.orientation)
      return up
    }

    Also remove old getUp() implementation that used cross product.
    vec3.transformQuat() rotates local basis vectors by quaternion, giving local camera axes.

    Important: Use local up from quaternion, NOT fixed world up [0, 1, 0].
    This fixes gimbal lock - movement always uses camera's local coordinate space.
  </action>
  <verify>grep -E "transformQuat" src/core/Camera.ts shows getForward/getRight/getUp using quaternion</verify>
  <done>Vector calculations use vec3.transformQuat() with quaternion, not Euler formulas</done>
</task>

</tasks>

<verification>
- Camera.ts imports quat from Math.ts
- orientation field is quaternion type
- handleMouseMove() uses quat.rotateX/Y with normalization
- getForward/getRight/getUp use vec3.transformQuat()
- No Euler angle rotation formulas remain
</verification>

<success_criteria>
Camera uses quaternion-based rotation with accumulation and local vector calculation
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-implement-quaternion-based-camera/01.1-03-SUMMARY.md`
</output>
