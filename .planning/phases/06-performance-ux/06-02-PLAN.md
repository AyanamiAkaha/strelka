---
phase: 06-performance-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/views/WebGLPlayground.vue]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "WebGL buffers are deleted when component unmounts"
    - "WebGL shader program is deleted when component unmounts"
    - "GPU memory is freed when component unmounts"
    - "No GPU memory leaks on component unmount/remount cycles"
  artifacts:
    - path: "src/views/WebGLPlayground.vue"
      provides: "WebGL resource cleanup in onUnmounted()"
      contains: "onUnmounted", "deleteBuffer", "deleteProgram"
      min_lines: 630
  key_links:
    - from: "src/views/WebGLPlayground.vue"
      to: "onUnmounted() hook"
      via: "explicit WebGL resource deletion"
      pattern: "gl\\.deleteBuffer\\(positionBuffer\\.value\\)"
---

<objective>
Add comprehensive WebGL resource cleanup to onUnmounted() to prevent GPU memory leaks.

Purpose: Explicitly delete all WebGL resources (buffers, shader program) when component unmounts. JavaScript GC does not free GPU memory. Output: onUnmounted() hook with complete WebGL resource deletion.
</objective>

<execution_context>
@/home/frater260/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/frater260/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-performance-ux/06-RESEARCH.md

@src/views/WebGLPlayground.vue
</context>

<tasks>

<task type="auto">
  <name>Add WebGL resource cleanup to onUnmounted()</name>
  <files>src/views/WebGLPlayground.vue</files>
  <action>
    In the onUnmounted() hook (line 421), add comprehensive WebGL resource cleanup before the cancelAnimationFrame() call.

    After getting glCache reference, delete all WebGL resources in reverse order of creation:
    1. Delete shader program (depends on shaders)
    2. Call shaderManager.cleanup() to delete shader objects
    3. Delete position buffer
    4. Delete cluster ID buffer
    5. Null out all resource references

    Pattern:
    const gl = glCache
    if (!gl) return

    if (shaderProgram) {
      gl.deleteProgram(shaderProgram)
      shaderProgram = null
    }
    if (shaderManager) {
      shaderManager.cleanup()
      shaderManager = null
    }
    if (positionBuffer) {
      gl.deleteBuffer(positionBuffer)
      positionBuffer = null
    }
    if (clusterIdBuffer) {
      gl.deleteBuffer(clusterIdBuffer)
      clusterIdBuffer = null
    }

    This prevents GPU memory leaks when component unmounts.
  </action>
  <verify>
    Browser DevTools Performance → Memory shows no memory increase on component unmount/remount cycles, console has no WebGL errors.
  </verify>
  <done>
    onUnmounted() deletes all WebGL resources (buffers, shader program) before component unmounts, preventing GPU memory leaks.
  </done>
</task>

</tasks>

<verification>
- onUnmounted() deletes shader program
- onUnmounted() calls shaderManager.cleanup()
- onUnmounted() deletes position buffer
- onUnmounted() deletes cluster ID buffer
- All resource references are nulled after deletion
- No GPU memory increase on repeated unmount/remount cycles
</verification>

<success_criteria>
All WebGL resources (buffers, shader program, shaders) are explicitly deleted in onUnmounted(), preventing GPU memory leaks on component unmount.
</success_criteria>

<output>
After completion, create `.planning/phases/06-performance-ux/06-02-SUMMARY.md`
</output>
