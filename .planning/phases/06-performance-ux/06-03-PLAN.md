---
phase: 06-performance-ux
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified: [src/views/WebGLPlayground.vue, src/components/DataLoadControl.vue]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "WebGLPlayground owns isLoading state (single source of truth)"
    - "DataLoadControl receives isLoading as prop (read-only)"
    - "No duplicate isLoading state between components"
    - "Loading overlay shows consistently across all components"
  artifacts:
    - path: "src/views/WebGLPlayground.vue"
      provides: "Single isLoading state source"
      contains: "isLoading = ref(false)"
      min_lines: 603
    - path: "src/components/DataLoadControl.vue"
      provides: "isLoading prop (read-only)"
      contains: "isLoading: boolean"
      min_lines: 180
  key_links:
    - from: "src/views/WebGLPlayground.vue"
      to: "src/components/ControlsOverlay.vue"
      via: ":is-loading prop"
      pattern: ":is-loading=\"isLoading\""
    - from: "src/components/ControlsOverlay.vue"
      to: "src/components/DataLoadControl.vue"
      via: ":is-loading prop"
      pattern: ":is-loading=\"isLoading\""
---

<objective>
Unify loading state between DataLoadControl and WebGLPlayground to eliminate duplicate state and UI inconsistency.

Purpose: Remove duplicate isLoading state across components, establish single source of truth in parent (WebGLPlayground), pass to child (DataLoadControl) as prop. Output: Parent owns loading state, child receives via prop.
</objective>

<execution_context>
@/home/frater260/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/frater260/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-performance-ux/06-RESEARCH.md

@src/views/WebGLPlayground.vue
@src/components/DataLoadControl.vue
@src/components/ControlsOverlay.vue
</context>

<tasks>

<task type="auto">
  <name>Remove isLoading state from DataLoadControl</name>
  <files>src/components/DataLoadControl.vue</files>
  <action>
    In DataLoadControl.vue:
    1. Remove local isLoading ref (line 57: const isLoading = ref(false))
    2. Add isLoading to props definition:
       defineProps<{
         isLoading: boolean
         file: File | null
       }>()
    3. Replace all references to isLoading with props.isLoading in template and script
    4. Remove all isLoading.value = true/false assignments (parent controls state now)
    5. Remove isLoading.value from watch in processFile() - parent handles loading state

    DataLoadControl now receives isLoading as read-only prop instead of managing its own state.
  </action>
  <verify>
    DataLoadControl template uses props.isLoading, no local isLoading ref exists, no isLoading.value assignments in script.
  </verify>
  <done>
    DataLoadControl receives isLoading as prop, no local state management, eliminates duplicate loading state.
  </done>
</task>

<task type="auto">
  <name>Pass isLoading through ControlsOverlay to DataLoadControl</name>
  <files>src/views/WebGLPlayground.vue, src/components/ControlsOverlay.vue</files>
  <action>
    In WebGLPlayground.vue, isLoading is already passed to ControlsOverlay as prop (line 17).
    In ControlsOverlay.vue, isLoading is already received as prop (line 87) and passed to DataLoadControl (line 8).

    Verify the prop chain is complete:
    WebGLPlayground.vue → :is-loading="isLoading" → ControlsOverlay.vue (props.isLoading)
    ControlsOverlay.vue → :is-loading="isLoading" → DataLoadControl.vue (props.isLoading)

    The prop chain is already correctly implemented, only DataLoadControl needed updates.
  </action>
  <verify>
    isLoading prop flows from WebGLPlayground → ControlsOverlay → DataLoadControl without duplication.
  </verify>
  <done>
    Loading state is unified: parent WebGLPlayground owns state, passes through ControlsOverlay to DataLoadControl as prop.
  </done>
</task>

</tasks>

<verification>
- DataLoadControl has no local isLoading ref
- DataLoadControl receives isLoading as prop (defineProps<{ isLoading: boolean }>())
- WebGLPlayground owns isLoading state (ref)
- isLoading prop flows parent → child through ControlsOverlay
- No duplicate isLoading state management
- Loading overlay shows consistently when loading
</verification>

<success_criteria>
Loading state is unified with single source of truth in WebGLPlayground, passed to DataLoadControl as read-only prop, eliminating duplicate state and UI inconsistency.
</success_criteria>

<output>
After completion, create `.planning/phases/06-performance-ux/06-03-SUMMARY.md`
</output>
