---
phase: 02-json-data-loader
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/core/DataProvider.ts]
autonomous: true

must_haves:
  truths:
    - "DataProvider has loadFromFile() method accepting File object"
    - "Method uses FileReader to read file as text"
    - "Method calls parseJsonData() for validation and conversion"
    - "Method returns Promise<PointData> for async file reading"
    - "FileReader errors are caught and logged"
  artifacts:
    - path: "src/core/DataProvider.ts"
      provides: "JSON file loading method"
      exports: ["loadFromFile"]
      contains: "static async loadFromFile"
  key_links:
    - from: "src/core/DataProvider.ts"
      to: "src/core/validators.ts"
      via: "import parseJsonData"
      pattern: "import.*parseJsonData.*from.*validators"
    - from: "src/core/DataProvider.ts"
      to: "FileReader API"
      via: "reader.readAsText(file)"
      pattern: "readAsText"
---

<objective>
Add loadFromFile() method to DataProvider that reads JSON files using FileReader and delegates validation/parsing to parseJsonData(). Provides async interface for Vue components to load files.

Purpose: Bridge file reading with validation logic, integrate with existing DataProvider
Output: DataProvider.loadFromFile() method
</objective>

<execution_context>
@/home/frater260/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/frater260/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-json-data-loader/02-CONTEXT.md
@.planning/phases/02-json-data-loader/02-RESEARCH.md

@src/core/DataProvider.ts
@.planning/phases/02-json-data-loader/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add loadFromFile() method to DataProvider</name>
  <files>src/core/DataProvider.ts</files>
  <action>
    Modify src/core/DataProvider.ts:

    1. Add import at top: import { parseJsonData } from './validators'

    2. Add new static method after getPointData():

       ```typescript
       static async loadFromFile(file: File): Promise<PointData> {
         const reader = new FileReader()
         return new Promise((resolve, reject) => {
           reader.onload = (e) => {
             try {
               const content = e.target?.result as string
               const pointData = parseJsonData(content)
               resolve(pointData)
             } catch (error) {
               reject(error)
             }
           }
           reader.onerror = () => {
             const error = new Error('Failed to read file')
             console.error('FileReader error:', reader.error)
             reject(error)
           }
           reader.readAsText(file)
         })
       }
       ```

    3. Remove the commented-out loadPointDataFromFile(url: string) example if present (lines 93-124)

    Follow RESEARCH.md Pattern 1 (File Input with FileReader) and RESEARCH.md Pitfall 3 (fresh FileReader instance each time)
  </action>
  <verify>
    grep -q "static async loadFromFile" src/core/DataProvider.ts && \
    grep -q "parseJsonData" src/core/DataProvider.ts && \
    ! grep -q "loadPointDataFromFile.*url.*string" src/core/DataProvider.ts
  </verify>
  <done>loadFromFile() reads JSON files and returns PointData</done>
</task>

</tasks>

<verification>
- loadFromFile() accepts File parameter and returns Promise<PointData>
- Method imports and calls parseJsonData() from validators.ts
- FileReader instance created per call (not reused)
- FileReader errors logged to console.error() with reader.error
- Old commented code removed (loadPointDataFromFile example)
- Method integrates cleanly with existing DataProvider class
</verification>

<success_criteria>
DataProvider can load and parse JSON files asynchronously. FileReader errors caught and re-thrown. Validation and conversion delegated to parseJsonData(). Ready for Vue component integration.
</success_criteria>

<output>
After completion, create `.planning/phases/02-json-data-loader/02-02-SUMMARY.md`
</output>
