---
phase: 12-fix-v1.2-integration-bugs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/views/WebGLPlayground.vue]
autonomous: true

must_haves:
  truths:
    - "JSON data with tag loads and calculates hover thresholds without ReferenceError"
    - "Shader receives cursor world position with all 3 components (x, y, z)"
    - "Edge clamping preserves 15px gap between overlay and point for all screen positions"
    - "All 3 E2E flows work correctly: JSON with tag, SQLite with image, no metadata"
  artifacts:
    - path: "src/views/WebGLPlayground.vue"
      provides: "Fixed integration bugs"
      contains: "calculatePointDensityThresholds(loadedData.positions, pointCount.value)"
  key_links:
    - from: "src/views/WebGLPlayground.vue:215"
      to: "loadedData.positions"
      via: "Variable reference fix"
      pattern: "calculatePointDensityThresholds\\(loadedData\\.positions"
    - from: "src/views/WebGLPlayground.vue:668-672"
      to: "shader uniform u_cursorWorldPos"
      via: "vec3 uniform call"
      pattern: "gl\\.uniform3f"
    - from: "src/views/WebGLPlayground.vue:587"
      to: "edge clamping calculation"
      via: "15px offset adjustment"
      pattern: "Math\\.max\\(overlayHeight\\s*\\+\\s*15"
---

<objective>
Fix 3 critical integration bugs identified in v1.2 milestone audit that prevent end-to-end flows from working correctly.

Purpose: Resolve variable name mismatch, shader uniform type mismatch, and edge clamping calculation error
Output: Working end-to-end flows for JSON with tag, SQLite with image, and no metadata scenarios
</objective>

<execution_context>
@/home/frater260/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/frater260/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Bug locations in src/views/WebGLPlayground.vue:
# Line 215: Variable name mismatch (result should be loadedData)
# Lines 668-672: Shader expects vec3 but code uses uniform2f with only 2 values
# Line 587: Edge clamping doesn't account for 15px vertical offset in CSS transform
</context>

<tasks>

<task type="auto">
  <name>Fix JSON data loading ReferenceError</name>
  <files>src/views/WebGLPlayground.vue</files>
  <action>
    On line 215, change `result.pointData.positions` to `loadedData.positions` in the calculatePointDensityThresholds call.

    Current incorrect code:
    ```
    const thresholds = calculatePointDensityThresholds(result.pointData.positions, pointCount.value)
    ```

    Corrected code:
    ```
    const thresholds = calculatePointDensityThresholds(loadedData.positions, pointCount.value)
    ```

    This fixes the ReferenceError that occurs when loading JSON data with tag column because `result` is undefined. The variable `loadedData` contains the parsed JSON data with positions array.
  </action>
  <verify>Read line 215 and confirm it references `loadedData.positions` not `result.pointData.positions`</verify>
  <done>calculatePointDensityThresholds uses correct variable reference `loadedData.positions`</done>
</task>

<task type="auto">
  <name>Fix shader uniform type mismatch for cursor world position</name>
  <files>src/views/WebGLPlayground.vue</files>
  <action>
    On lines 668-672, change `gl.uniform2f` to `gl.uniform3f` and add the third component (worldPos.z) to match the shader's vec3 uniform type.

    Current incorrect code:
    ```
    gl.uniform2f(
      gl.getUniformLocation(shaderProgram, 'u_cursorWorldPos'),
      worldPos.x,
      worldPos.y
    )
    ```

    Corrected code:
    ```
    gl.uniform3f(
      gl.getUniformLocation(shaderProgram, 'u_cursorWorldPos'),
      worldPos.x,
      worldPos.y,
      worldPos.z
    )
    ```

    This fixes the mismatch where the shader expects a vec3 (x, y, z) but the code only passes 2 values (x, y). The shader uniform `u_cursorWorldPos` is declared as `uniform vec3 u_cursorWorldPos;` in the vertex shader.
  </action>
  <verify>Read lines 668-673 and confirm it uses `gl.uniform3f` with 3 arguments (worldPos.x, worldPos.y, worldPos.z)</verify>
  <done>shader receives all 3 components of cursor world position via gl.uniform3f</done>
</task>

<task type="auto">
  <name>Fix edge clamping to preserve 15px gap</name>
  <files>src/views/WebGLPlayground.vue</files>
  <action>
    On line 587, adjust the clamping calculation to account for the 15px vertical offset from the CSS transform. The CSS uses `transform: translate(-50%, calc(-100% - 15px))` which positions the overlay 15px above the point. Therefore, when clamping to prevent going off the top of the screen, we must ensure `overlayHeight + 15` remains visible, not just `overlayHeight`.

    Current incorrect code:
    ```
    const clampedY = Math.max(overlayHeight, Math.min(desiredY, canvas.height));
    ```

    Corrected code:
    ```
    const clampedY = Math.max(overlayHeight + 15, Math.min(desiredY, canvas.height));
    ```

    This ensures that the overlay's bottom edge (after the 15px offset) stays within the viewport, preserving the intended 15px gap between the overlay and the point.
  </action>
  <verify>Read line 587 and confirm it uses `overlayHeight + 15` in the Math.max call</verify>
  <done>edge clamping preserves 15px gap between overlay and point for all screen positions</done>
</task>

<task type="auto">
  <name>Optional cleanup: Remove console.log debug statement</name>
  <files>src/views/WebGLPlayground.vue</files>
  <action>
    Remove the `console.log('Loaded point data:', { pointCount: pointCount.value, columns: loadedData.columns })` statement that was likely added for debugging during phase 11-04 development.

    Search for this console.log around the data loading section (near line 220-230) and remove the entire line.

    SKIP GUIDANCE: This is optional cleanup - skip this task if focused on core bug fixes. The debug statement does not affect functionality.
  </action>
  <verify>Run `grep -n "Loaded point data" src/views/WebGLPlayground.vue` and confirm no matches found</verify>
  <done>Debug console.log statement removed from codebase</done>
</task>

<task type="checkpoint:human-verify">
  <name>Verify all 3 E2E flows work correctly</name>
  <what-built>Fixed integration bugs: JSON loading, shader uniform type, edge clamping</what-built>
  <how-to-verify>
    Test all 3 end-to-end flows:

    1. **JSON with tag flow**:
       - Load a JSON file with tag column
       - Verify no ReferenceError occurs in console
       - Hover over points and confirm tag displays in overlay
       - Move cursor to screen edges - overlay stays within viewport

    2. **SQLite with image flow**:
       - Load a SQLite file with image column
       - Verify data loads successfully without errors
       - Hover over points and confirm image displays in overlay
       - Move cursor to screen edges - overlay stays within viewport with 15px gap from point

    3. **No metadata flow**:
       - Load data without tag/image columns
       - Verify hover detection still works (no ReferenceError)
       - No overlay displays (expected behavior)

    For edge clamping verification (all flows):
    - Hover over points near all four screen edges (top, bottom, left, right)
    - Confirm overlay always stays within viewport with 15px gap from the point
    - Confirm overlay never gets clipped or partially outside viewport
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 E2E flows work correctly, or describe issues</resume-signal>
  <done>all 3 E2E flows work: JSON with tag, SQLite with image, and no metadata scenarios</done>
</task>

</tasks>

<verification>
Test all 3 end-to-end flows after fixes:

1. **JSON with tag flow**: Load a JSON file with tag column, verify no ReferenceError occurs, hover over points and confirm tag displays in overlay with proper edge clamping
2. **SQLite with image flow**: Load a SQLite file with image column, verify hover works and image displays in overlay with proper edge clamping
3. **No metadata flow**: Load data without tag/image columns, verify hover detection still works (no ReferenceError, no overlay needed)

For edge clamping verification:
- Hover over points near all four screen edges (top, bottom, left, right)
- Confirm overlay always stays within viewport with 15px gap from the point
- Confirm overlay never gets clipped or partially outside viewport
</verification>

<success_criteria>
All 3 end-to-end flows work correctly:
- JSON data with tag loads and calculates hover thresholds without ReferenceError
- Shader receives cursor world position with all 3 components (x, y, z)
- Edge clamping preserves 15px gap between overlay and point for all screen positions
- All flows pass visual verification with proper hover detection and overlay display
</success_criteria>

<output>
After completion, create `.planning/phases/12-fix-v1.2-integration-bugs/12-01-SUMMARY.md`
</output>
