---
phase: 09-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/types.ts
  - src/core/validators.ts
  - src/core/DataProvider.ts
autonomous: true

must_haves:
  truths:
    - User can load JSON data with tag column and tags are accessible in loaded data
    - User can load JSON data with image column and image URLs are accessible in loaded data
    - User can load data without tag/image columns and system works without errors or warnings
    - User can load SQLite data with tag/image columns and columns are accessible in loaded data
  artifacts:
    - path: src/core/types.ts
      provides: Extended JsonPoint with tag?, TableInfo with hasTag/hasImage, PointData with tagIndices/imageIndices/tagLookup/imageLookup
      contains: "tag?: string | null"
    - path: src/core/validators.ts
      provides: validateJsonPoint() extended for tag/image validation, parseJsonData() with detection and loading of optional columns
      exports: ["validateJsonPoint", "parseJsonData", "validateTableSchema"]
    - path: src/core/DataProvider.ts
      provides: PointData type extended with tag/image fields, loadSqliteFile() with detection and loading of optional columns
      exports: ["loadFromFile", "loadSqliteFile"]
  key_links:
    - from: src/core/validators.ts
      to: src/core/types.ts
      via: import of JsonPoint, TableInfo
      pattern: "import.*JsonPoint.*from.*types"
    - from: src/core/validators.ts
      to: src/core/DataProvider.ts
      via: import of parseJsonData(), validateTableSchema()
      pattern: "import.*parseJsonData.*from.*validators"
    - from: src/core/DataProvider.ts
      to: src/core/types.ts
      via: PointData type definition
      pattern: "export interface PointData"
---

<objective>
Extend data loading infrastructure to support optional `tag` and `image` columns from JSON and SQLite data, with graceful degradation for missing data.

Purpose: Foundation for Phase 10 (GPU hover detection) and Phase 11 (screen overlay) by ensuring tag/image metadata can be loaded and accessed without breaking existing functionality or requiring changes to existing data formats.

Output: Extended type definitions, validation logic, and data loaders that support optional tag/image columns with null handling and schema detection.
</objective>

<execution_context>
@/home/frater260/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/frater260/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-data-foundation/09-RESEARCH.md
@.planning/phases/09-data-foundation/09-CONTEXT.md
@src/core/types.ts
@src/core/validators.ts
@src/core/DataProvider.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend type definitions and JSON validation/parsing</name>
  <files>src/core/types.ts, src/core/validators.ts</files>
  <action>
    In src/core/types.ts:
    1. Extend JsonPoint interface to add optional fields:
       - tag?: string | null
       - image?: string | null
    2. Extend TableInfo interface to add:
       - hasTag: boolean
       - hasImage: boolean

    In src/core/validators.ts:
    3. Extend validateJsonPoint() to validate tag and image fields if present:
       - Check if 'tag' in p: validate that p.tag is string, null, or undefined
       - Check if 'image' in p: validate that p.image is string, null, or undefined
       - Return error message if type validation fails
       - Note: No required check - these are optional fields

    4. Extend parseJsonData() to detect and load optional columns:
       - After validation loop, detect columns: check 'tag' in data[0] and 'image' in data[0]
       - Create normalizeOptionalValue() helper: returns null if value is undefined, null, or ""
       - Create Map<string, number> for tags and images to store unique values with indices
       - First pass: populate maps with unique non-null tag/image values from data array
       - Allocate Float32Arrays for tagIndices and imageIndices if any values exist, else null
       - Second pass: fill positions, clusterIds, tagIndices, imageIndices arrays
       - Return PointData with:
         * positions (Float32Array)
         * clusterIds (Float32Array)
         * tagIndices (Float32Array | null) - index into tagLookup or -1
         * imageIndices (Float32Array | null) - index into imageLookup or -1
         * tagLookup (Map<string, number> | null)
         * imageLookup (Map<string, number> | null)
         * count (number)

    Implementation approach from RESEARCH.md:
    - Use index-based storage (Float32Array + Map lookup) not string[]
    - Normalize null/undefined/empty to null before storing
    - Silent degradation: no console.warn when columns missing
  </action>
  <verify>
    TypeScript compilation: npx tsc --noEmit
    Manual test: Load JSON with tag column, check tagIndices is populated
    Manual test: Load JSON without tag column, check tagIndices is null (no errors)
  </verify>
  <done>
    JsonPoint has tag?: string | null and image?: string | null
    TableInfo has hasTag and hasImage boolean fields
    validateJsonPoint() validates tag/image types if present
    parseJsonData() detects optional columns and returns tagIndices/imageIndices/tagLookup/imageLookup
    JSON with tag/image: tagIndices/imageIndices populated with indices, maps contain values
    JSON without tag/image: tagIndices/imageIndices are null, maps are null, no errors/warnings
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend SQLite schema validation</name>
  <files>src/core/validators.ts</files>
  <action>
    In src/core/validators.ts, extend validateTableSchema():

    1. After checking for required columns (x, y, z) and hasCluster:
       - Check if 'tag' exists in columnNames: const hasTag = columnNames.includes('tag')
       - Check if 'image' exists in columnNames: const hasImage = columnNames.includes('image')

    2. Update TableInfo return object to include:
       - name: tableName
       - hasCluster: columnNames.includes('cluster')
       - hasTag: boolean (detected above)
       - hasImage: boolean (detected above)

    Implementation notes from RESEARCH.md:
    - Use PRAGMA table_info results (already queried)
    - Extract columnNames using schemaResult.values.map()
    - Silent detection: no errors if columns missing
    - Backward compatible: existing SQLite files without tag/image still work
  </action>
  <verify>
    TypeScript compilation: npx tsc --noEmit
    Manual test: Call validateTableSchema(db, "table_with_tag") returns hasTag: true
    Manual test: Call validateTableSchema(db, "table_without_tag") returns hasTag: false
  </verify>
  <done>
    validateTableSchema() returns TableInfo with hasTag and hasImage boolean fields
    SQLite tables with tag column: hasTag is true
    SQLite tables without tag column: hasTag is false (no error)
    Same logic for image column
    Backward compatible with existing SQLite schema validation
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend SQLite loading</name>
  <files>src/core/DataProvider.ts</files>
  <action>
    In src/core/DataProvider.ts:

    1. Extend PointData interface to add optional fields:
       - tagIndices?: Float32Array | null
       - imageIndices?: Float32Array | null
       - tagLookup?: Map<string, number> | null
       - imageLookup?: Map<string, number> | null

    2. Extend loadSqliteFile() to detect and load optional columns:
       - After validateTableSchema(), extract hasTag and hasImage from tableInfo
       - Create Map<string, number> for tags and images to store unique values with indices
       - Allocate Float32Arrays for positions, clusterIds (existing code)
       - Allocate tagIndices Float32Array if hasTag && any tag values not null, else null
       - Allocate imageIndices Float32Array if hasImage && any image values not null, else null
       - Build dynamic SELECT query with optional columns:
         * Base: `SELECT x, y, z${tableInfo.hasCluster ? ', cluster' : ''}`
         * Add tag: `, tag` if tableInfo.hasTag
         * Add image: `, image` if tableInfo.hasImage
         * Full: `SELECT x, y, z${tableInfo.hasCluster ? ', cluster' : ''}${hasTag ? ', tag' : ''}${hasImage ? ', image' : ''} FROM ${tableName}`
       - In db.each() callback, process each row:
         * Fill positions[index * 3], positions[index * 3 + 1], positions[index * 3 + 2]
         * Fill clusterIds[index]
         * If hasTag and row.tag exists: normalize and add to tags map, store index in tagIndices
         * If hasImage and row.image exists: normalize and add to images map, store index in imageIndices
       - On completion, return PointData with all fields

    3. Reuse or copy normalizeOptionalValue() helper from validators.ts:
       - Function signature: function normalizeOptionalValue(value: unknown): string | null
       - Returns null if value is undefined, null, or ""
       - Returns String(value) otherwise

    Implementation approach from RESEARCH.md:
    - Use index-based storage (Float32Array + Map lookup) not string[]
    - Dynamic SELECT query prevents "no such column" errors
    - Normalize null/undefined/empty to null before storing in maps
    - Silent degradation: no console.warn when columns missing
  </action>
  <verify>
    TypeScript compilation: npx tsc --noEmit
    Manual test: Load SQLite with tag column, check tagIndices is populated
    Manual test: Load SQLite without tag column, check tagIndices is null (no errors)
    Manual test: Load SQLite with empty tag values, check normalized to null (index -1)
  </verify>
  <done>
    PointData interface extended with tagIndices/imageIndices/tagLookup/imageLookup fields
    loadSqliteFile() uses dynamic SELECT with optional columns
    SQLite with tag/image: tagIndices/imageIndices populated with indices, maps contain values
    SQLite without tag/image: tagIndices/imageIndices are null, maps are null, no errors/warnings
    SQLite with null/empty tag/image: normalized to null, stored as -1 in index arrays
    Backward compatible: existing SQLite files load without errors
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. TypeScript compilation passes: npx tsc --noEmit
2. Load existing JSON files (without tag/image): loads successfully, no errors or warnings
3. Load existing SQLite files (without tag/image): loads successfully, no errors or warnings
4. Load JSON with tag column: tagIndices is Float32Array with indices, tagLookup Map contains values
5. Load JSON with image column: imageIndices is Float32Array with indices, imageLookup Map contains values
6. Load SQLite with tag column: tagIndices is Float32Array with indices, tagLookup Map contains values
7. Load SQLite with image column: imageIndices is Float32Array with indices, imageLookup Map contains values
8. Test normalization: tag/image values of null, undefined, "" all result in -1 in index arrays
9. Verify maps: tagLookup and imageLookup contain only non-null, non-empty string values
</verification>

<success_criteria>
Type definitions extended with optional tag/image fields
JSON loader detects and loads optional tag/image columns with index-based storage
SQLite schema validation detects optional tag/image columns
SQLite loader uses dynamic SELECT to safely load optional columns with index-based storage
All loaders normalize null/undefined/empty to null and use silent degradation
Existing JSON/SQLite files without tag/image load without errors or warnings
Data with tag/image columns: tagIndices/imageIndices populated, tagLookup/imageLookup contain values
Data without tag/image columns: tagIndices/imageIndices are null, tagLookup/imageLookup are null
</success_criteria>

<output>
After completion, create `.planning/phases/09-data-foundation/09-01-SUMMARY.md`
</output>
