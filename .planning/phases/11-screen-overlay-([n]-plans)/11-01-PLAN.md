---
phase: 11-screen-overlay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/WebGLPlayground.vue
autonomous: true

must_haves:
  truths:
    - "User can hover over a point and system tracks which point is hovered (point index)"
    - "When hovering a point with tag data, the tag value is retrieved from tagLookup Map"
    - "When hovering a point with image data, the image URL is retrieved from imageLookup Map"
    - "Hovered point identification matches visual highlighting from Phase 10 (same thresholds)"
    - "System handles points without tag/image data gracefully (no overlay shown)"
  artifacts:
    - path: "src/views/WebGLPlayground.vue"
      provides: "Hovered point state tracking and metadata retrieval"
      min_lines: 50
      contains: "hoveredPointIndex", "hoveredPointTag", "hoveredPointImage", "findHoveredPointIndex"
  key_links:
    - from: "src/views/WebGLPlayground.vue"
      to: "src/core/DataProvider.ts"
      via: "pointData.tagLookup.get() and pointData.imageLookup.get()"
      pattern: "tagLookup|imageLookup"
    - from: "src/views/WebGLPlayground.vue"
      to: "src/core/ShaderManager.ts"
      via: "Same distance thresholds as shader (cameraDistThreshold, cursorDistThreshold)"
      pattern: "cameraDistThreshold|cursorDistThreshold"
---

<objective>
Add CPU-side hovered point identification system that tracks which point is under the cursor and retrieves tag/image metadata from Map-based storage.

Purpose: Phase 10 provides GPU-based visual highlighting but does not return point index to JavaScript. This plan adds CPU-side point selection using same distance thresholds, enabling metadata retrieval for overlay display.

Output: Hovered point state (index, tag, image) updated every frame, ready for world-to-screen projection and overlay rendering.
</objective>

<execution_context>
@/home/frater260/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/frater260/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-gpu-hover-detection/10-03-SUMMARY.md
@.planning/phases/11-screen-overlay-(n)-plans/11-CONTEXT.md
@.planning/phases/11-screen-overlay-(n)-plans/11-RESEARCH.md
@src/views/WebGLPlayground.vue
@src/core/DataProvider.ts
@src/core/ShaderManager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hovered point state tracking</name>
  <files>src/views/WebGLPlayground.vue</files>
  <action>
    Add three reactive refs to track hovered point state:
    - hoveredPointIndex: Ref<number> (default -1, meaning no point hovered)
    - hoveredPointTag: Ref<string | null> (default null)
    - hoveredPointImage: Ref<string | null> (default null)

    Place these after the existing hoverThresholds ref definition (around line 141).

    Reference Phase 10-03 SUMMARY for existing hover state pattern.
  </action>
  <verify>
    File contains const hoveredPointIndex = ref(-1), const hoveredPointTag = ref(null), const hoveredPointImage = ref(null)
  </verify>
  <done>
    Hovered point state variables defined and initialized with default values
  </done>
</task>

<task type="auto">
  <name>Task 2: Add findHoveredPointIndex() function</name>
  <files>src/views/WebGLPlayground.vue</files>
  <action>
    Add function that identifies which point is hovered using the same distance thresholds as the shader:

    function findHoveredPointIndex(
      pointData: PointData,
      cameraPos: vec3,
      cursorWorldPos: {x: number, y: number, z: number},
      cameraDistThreshold: number,
      cursorDistThreshold: number
    ): number {
      let bestIndex = -1;
      let bestDist = Infinity;

      for (let i = 0; i < pointData.count; i++) {
        const idx = i * 3;
        const px = pointData.positions[idx];
        const py = pointData.positions[idx + 1];
        const pz = pointData.positions[idx + 2];

        const distToCamera = Math.sqrt(
          Math.pow(cameraPos[0] - px, 2) +
          Math.pow(cameraPos[1] - py, 2) +
          Math.pow(cameraPos[2] - pz, 2)
        );
        const cameraNear = distToCamera < cameraDistThreshold;

        const distToCursor = Math.sqrt(
          Math.pow(cursorWorldPos.x - px, 2) +
          Math.pow(cursorWorldPos.y - py, 2) +
          Math.pow(cursorWorldPos.z - pz, 2)
        );
        const cursorNear = distToCursor < cursorDistThreshold;

        if (cameraNear && cursorNear) {
          if (distToCursor < bestDist) {
            bestDist = distToCursor;
            bestIndex = i;
          }
        }
      }

      return bestIndex;
    }

    Place this function after the calculatePointDensityThresholds() function.
    Use the same two-distance threshold logic as the shader.
    Return -1 if no point meets both criteria.

    Reference Phase 10-01 SUMMARY for shader hover detection logic.
  </action>
  <verify>
    Function exists with signature matching implementation, contains two-distance threshold logic
  </verify>
  <done>
    Function identifies hovered point index using same thresholds as shader, returns -1 if no point hovered
  </done>
</task>

<task type="auto">
  <name>Task 3: Update hover state in render loop</name>
  <files>src/views/WebGLPlayground.vue</files>
  <action>
    In the render loop (inside render() function, after updating camera position and before clearing canvas):

    1. Call findHoveredPointIndex() to identify the hovered point
    2. Update hoveredPointIndex ref with the result
    3. Retrieve tag/image metadata from PointData's tagLookup and imageLookup Maps

    Add this code after camera.value.update() (around line 398).

    Reference Phase 09 SUMMARY for PointData structure.
    Reference Phase 10-03 SUMMARY for existing world position conversion logic.

    Use reverse lookup for Maps (Map stores string -> index, we need index -> string).
  </action>
  <verify>
    Render loop contains hover state update logic after camera.update(), metadata retrieved from Maps
  </verify>
  <done>
    Hover state updated every frame with point index and metadata (tag, image) from Map-based storage
  </done>
</task>

</tasks>

<verification>
- [ ] Hovered point state variables defined (hoveredPointIndex, hoveredPointTag, hoveredPointImage)
- [ ] findHoveredPointIndex() function uses same two-distance thresholds as shader
- [ ] Hover state updated in render loop every frame
- [ ] Metadata retrieved from tagLookup and imageLookup Maps
- [ ] Hover state cleared when no point is hovered
- [ ] Console logs show hover state updates when hovering points with/without metadata
</verification>

<success_criteria>
User can hover over points, and the system tracks which point is hovered (index) and retrieves tag/image metadata from Map-based storage. Hovered point identification matches the visual highlighting from Phase 10.
</success_criteria>

<output>
After completion, create `.planning/phases/11-screen-overlay-(n)-plans/11-01-SUMMARY.md`
</output>
